import { PaymentEntity } from "../entity";

export default class PaymentService {
	constructor(payment: PaymentEntity) {
		//todo function qui déteermine si isSuccess en fonction de !isFuture(Date.now(subimetedAT && exetuctedAt))))

	}
}